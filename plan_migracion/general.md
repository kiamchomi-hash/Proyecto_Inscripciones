# Tareas generales del proyecto

## Analytics y SEO

- Insertar Vercel Analytics — Agregar `@vercel/analytics` al proyecto y el componente `<Analytics />` en el layout principal
- Insertar Google Search Console — Agregar meta tag de verificación en el `<head>` del layout o archivo de verificación en `public/`
